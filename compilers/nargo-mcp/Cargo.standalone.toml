[package]
name = "nargo-mcp"
version = "0.1.0"
edition = "2024"
description = "Model Context Protocol (MCP) server for Nargo package manager"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.36", features = ["full"] }
tracing = "0.1"

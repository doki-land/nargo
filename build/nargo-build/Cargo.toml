[package]
name = "nargo-build"
version = "0.1.0"
description = "Nargo 构建系统 - 基于自研体系的前端构建系统，支持增量构建"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nargo-ir = { path = "../../compilers/nargo-ir" }
nargo-types = { path = "../../compilers/nargo-types" }

[dev-dependencies]

